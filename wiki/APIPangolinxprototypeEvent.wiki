#summary Class API for Event from module XPrototype
#labels Class-jplex.xprototype.Event,Release-Pangolin,Module-XPrototype,API-Class
#sidebar APIPangolinxprototypeIndex

= `static` Class *jplex.xprototype.Event*  =

== Description ==
jPlex's XPrototype extended Event




== Properties ==
<table border="1" cellspacing="0" cellpadding="5">        
<tr><td> *Name* </td><td> *Type* </td><td> *Description* </td></tr>
<tr><td> _Key_ </td><td> `object` </td><td> `static  `  Map of all key codes, including special characters</td></tr>

</table>



== Methods ==
<table border="1" cellspacing="0" cellpadding="5">                                
<tr><td> *Signature* </td><td> *Return Type* </td><td> *Description* </td></tr>
<tr><td> `onElementReady(element, handler)` </td><td> `void` </td><td> Execute the given handler as soon as the element is detected
in the DOM. It uses Prototype's PeriodicalExecuter to check
the presence every Event._ready_POLL_INTERVAL milliseconds.
If not found after Event._ready_POLL_LIMIT tries, we simply
give up.
By default this limit is set to 800 tries, with a 5 ms delay
between them (40s max.)
Note that 'this' refers to the Element (i.e. $(element))
itself in the callback function.<br />  - `element <String>` ID of the element we are waiting for<br/> - `handler <Function>` Associated callback.<br/></td></tr>

</table>
