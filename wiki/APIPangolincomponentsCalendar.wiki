#summary Class API for Calendar from module Components
#labels Class-jplex.components.Calendar,Release-Pangolin,Module-Components,API-Class
#sidebar APIPangolincomponentsIndex

= Class *jplex.components.Calendar*  _extends [APIPangolincommonComponent jplex.common.Component]_  =

== Description ==
Calendar component class.
Create a browsable calendar from a text field, a button or directly on the page.

<strong>Main features</strong>:
<ul>
<li>Link the calendar to a simple text input (appears on focus), to a button (appears on click)
and a text field. You could also use whichever control you want and specify the behavior with custom events.</li>
<li>Browse the calendar by month or using the 'fast browse'</li>
<li>Key shortcuts enabled: move the focused date using arrow keys or to
the previous/next month using PageDown/PageUp</li>
<li>Restrict the date to a chosen interval if you want</li>
<li>Custom events are fired at given steps of the component lifecycle (show/hide/select)</li>
<li>Use fade in/out from Script.aculo.us if you want so</li>
</ul>

== Config Table ==
<table border="1" cellspacing="0" cellpadding="5">
<tr><td> *Name* </td><td> *Default Value* </td><td> *Description* </td></tr>
<tr><td> _date_ </td><td> `new Date()` </td><td>  The default date to select </td></tr>
<tr><td> _dateFormat_ </td><td> `"d-m-Y"` </td><td>  Pattern to format the output string of the calendar
(e.g. the value of the linked text input field). To know
more about pattern tokens, see jPlex.xprototype.Date#format </td></tr>
<tr><td> _fade_ </td><td> `0.3` </td><td>  Time in seconds to show/hide the popup calendar.
Set to 0 or `false` to disable fade in/out. </td></tr>
<tr><td> _fastBrowse_ </td><td> `true` </td><td>  If set to `true`, a click on the title of the calendar (month or year)
will pop up a tooltip allowing the user to set a value for the month
(or the year) using combo-boxes </td></tr>
<tr><td> _fastBrowseYearEnd_ </td><td> `The current year + 5` </td><td>  Maximum year in the combobx for fast browse </td></tr>
<tr><td> _fastBrowseYearStart_ </td><td> `The current year - 5` </td><td>  Minimum year in the combobox for fast browse </td></tr>
<tr><td> _maxDate_ </td><td> `false` </td><td>  All dates that are above this one are not allowed </td></tr>
<tr><td> _minDate_ </td><td> `false` </td><td>  All dates that are bellow this one are not allowed </td></tr>
<tr><td> _source_ </td><td> `null` </td><td>  The event source, can be a textfield or a button for example
The source element for the calendar. Use one of the following configuration:
<ul>
<li>`textField = null, source = a text field`: links the calendar to a single text field</li>
<li>`textField = a text field, source = a button`: links the calendar to a button, the result
will be printed in the textfield</li>
<li>`textField = null, source = a button`: links the calendar to a button, use the custom event
onSelectEvent to catch the selected date and do what you want with it</li>
<li>`textField = null, source = null`: just show a calendar without show/hide things</li>
</ul> </td></tr>
<tr><td> _textField_ </td><td> `null` </td><td>  The textfield linked with the calendar (edited when a new date is selected)
(see the `source` configuration parameter for more details) </td></tr>
<tr><td> _titleFormat_ </td><td> `"{M} {Y}"` </td><td>  Template for the title of the calendar. You can use 3 tokens:
<ul><li>{M} the month full name</li>
<li>{m} month number</li>
<li>{Y} year with 4 digits</li></ul> </td></tr>
<tr><td> _zBase_ </td><td> `11000` </td><td>  zIndex base </td></tr>
</table>

== Event Table ==
<table border="1" cellspacing="0" cellpadding="5">
<tr><td> *Name* </td><td> *Description* </td><td> *Parameters* </td></tr>
<tr><td> _onHideEvent_ </td><td>  When the calendar disappears (textfield losing focus, new date selected for instance) </td><td>  </td></tr>
<tr><td> _onPositionChangeEvent_ </td><td>  Called each time the position of the calendar is re-computed </td><td>   - `position <Object>`  The computed position (position.top and position.left) <br/> - `dimensions <Object>`  Dimensions of the calendar container (width and height) <br/>  </td></tr>
<tr><td> _onSelectEvent_ </td><td>  When a new date is selected </td><td>   - `date <Date>`  The selected date <br/>  </td></tr>
<tr><td> _onShowEvent_ </td><td>  When the calendar appears (textfield receiving focus for instance) </td><td>  </td></tr>
</table>



== Methods ==
<table border="1" cellspacing="0" cellpadding="5">                                
<tr><td> *Signature* </td><td> *Return Type* </td><td> *Description* </td></tr>
<tr><td> `down()` </td><td> `void` </td><td> Navigate down in the current month, from the currently focused day.<br /> </td></tr>
<tr><td> `getFocusedItem()` </td><td> `Calendar.CalendarItem` </td><td> Get the currently focused item of the calendar<br /> </td></tr>
<tr><td> `getFormattedValue(format)` </td><td> `void` </td><td> Get the current date string representation, formatted with the given
pattern. If no pattern is given, uses the "dateFormat" configuration parameter.<br />  - `format <object>`<br/></td></tr>
<tr><td> `getSelectedItem()` </td><td> `Calendar.CalendarItem` </td><td> Get the currently selected item of the calendar<br /> </td></tr>
<tr><td> `getTextField()` </td><td> `Element` </td><td> Get the targetted text field of the calendar (null if not set)<br /> </td></tr>
<tr><td> `goTo(month, year)` </td><td> `void` </td><td> Change the page of the calendar to the specified month and year<br />  - `month <Integer>` New month (optional)<br/> - `year <Integer>` New year (optional)<br/></td></tr>
<tr><td> `hide()` </td><td> `void` </td><td> Makes the calendar disappear, with a fade out effect if configured so<br /> </td></tr>
<tr><td> `left()` </td><td> `void` </td><td> Navigate to the left in the current month, from the currently focused day.<br /> </td></tr>
<tr><td> `next()` </td><td> `void` </td><td> Go to the next month<br /> </td></tr>
<tr><td> `previous()` </td><td> `void` </td><td> Go to the previous month<br /> </td></tr>
<tr><td> `render()` </td><td> `void` </td><td> Render the calendar for the current month<br /> </td></tr>
<tr><td> `right()` </td><td> `void` </td><td> Navigate to the right in the current month, from the currently focused day.<br /> </td></tr>
<tr><td> `select(index)` </td><td> `void` </td><td> Explicitly selects the index'th cell in the calendar<br />  - `index <Integer>` Index of the cell to select (0 to oItems.length)<br/></td></tr>
<tr><td> `setFocusedItem(oItem)` </td><td> `void` </td><td> Sets the current focused item of the calendar (the one that would be selected)<br />  - `oItem <Calendar.CalendarItem>` calendar item to be marked as focused<br/></td></tr>
<tr><td> `setSelectedItem(oItem)` </td><td> `void` </td><td> Sets the current selected item of the calendar<br />  - `oItem <Calendar.CalendarItem>` calendar item to be marked as selected<br/></td></tr>
<tr><td> `show()` </td><td> `void` </td><td> Makes the calendar container appear, with a fade in effect if configured so<br /> </td></tr>
<tr><td> `up()` </td><td> `void` </td><td> Navigate up in the current month, from the currently focused day.<br /> </td></tr>

</table>
=== Inherited Methods ===

 * [APIPangolincommonComponent jplex.common.Component]:  `$C`,  `cfg`,  `fireEvent`,  `getEvent`,  `lang`,  `locale`,  `setCfg`,  `setEvent`,  `unregister`, 
