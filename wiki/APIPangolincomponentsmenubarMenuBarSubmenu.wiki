#summary Class API for MenuBarSubmenu from module Components
#labels Class-jplex.components.menubar.MenuBarSubmenu,Release-Pangolin,Module-Components,API-Class
#sidebar APIPangolincomponentsIndex

= `private` `static` Class *jplex.components.menubar.MenuBarSubmenu*  =

== Description ==
!MenuBar Submenu




== Properties ==
<table border="1" cellspacing="0" cellpadding="5">        
<tr><td> *Name* </td><td> *Type* </td><td> *Description* </td></tr>
<tr><td> _me_ </td><td> `Element` </td><td> `private  `  The `<ul>` HTML Element of the submenu</td></tr>

</table>



== Methods ==
<table border="1" cellspacing="0" cellpadding="5">                                
<tr><td> *Signature* </td><td> *Return Type* </td><td> *Description* </td></tr>
<tr><td> `addItem(item, before)` </td><td> `MenuBar.Submenu` </td><td> Adds the item oItem to the submenu.
If specified, adds it before the nBefore'th item<br />  - `item <Object>` Item to add (please refer to MenuBar's description above for item properties)<br/> - `before <int>` Optional. Position of the item (by default the item is pushed at the end)<br/></td></tr>
<tr><td> `getHTMLElement()` </td><td> `Element` </td><td> Get the `<ul>` element for the submenu<br /> </td></tr>
<tr><td> `getItem(index)` </td><td> `MenuBar.Submenu|bool` </td><td> Get the submenu of the item at position nIndex<br />  - `index <int>` Position of the item to get<br/></td></tr>
<tr><td> `getItems()` </td><td> `Array` </td><td> Returns the submenu items (array)<br /> </td></tr>
<tr><td> `getParentItem()` </td><td> `menubar.MenuBarItem` </td><td> Get the parent item of the submenu<br /> </td></tr>
<tr><td> `getRootMenuBar()` </td><td> `MenuBar` </td><td> Returns a reference to the root menu bar<br /> </td></tr>
<tr><td> `hide()` </td><td> `void` </td><td> Hides the submenu<br /> </td></tr>
<tr><td> `isEmpty()` </td><td> `bool` </td><td> Checks whether the submenu is empty or not<br /> </td></tr>
<tr><td> `removeItem(index)` </td><td> `menubar.MenuBarSubmenu|boolean` </td><td> Removes the index-th item of the submenu<br />  - `index <int>` position of the item to remove<br/></td></tr>
<tr><td> `render()` </td><td> `void` </td><td> Renders the submenu, with shade<br /> </td></tr>
<tr><td> `show()` </td><td> `void` </td><td> Makes the submenu visible<br /> </td></tr>

</table>
