#summary Class API for Dialog from module Components
#labels Class-jplex.components.frame.Dialog,Release-Pangolin,Module-Components,API-Class
#sidebar APIPangolincomponentsIndex

= Class *jplex.components.frame.Dialog*  _extends [APIPangolincomponentsFrame jplex.components.Frame]_  =

== Description ==
A preconfigured dialog box frame class<p>
It automatically adds two buttons to the footer of the frame: typically OK and Cancel.
You can add extra buttons if you want or remove the default ones, customize labels,...
All configuration paraameters from jplex.components.Frame are inherited. The following ones
are overridden to configure the Dialog frame:
<ul>
<li><code>header: true</code></li>
<li><code>center: true</code></li>
<li><code>draggable: true</code></li>
</ul>

== Config Table ==
|| *Name* || *Default Value* || *Description* ||
|| _cancelButton_ || `true` ||  Shows the "cancel" button if set to <code>true</code> ||
|| _cancelButtonLabel_ || `""` ||  Custom label for the 'cancel' button ||
|| _okButton_ || `true` ||  Shows the "ok" button if set to <code>true</code> ||
|| _okButtonLabel_ || `""` ||  Custom label for the 'OK' button ||
|| _otherButtons_ || `[]` ||  Array of optional button, each item of the array is an object like the following<br /><pre>{<br />&nbsp;&nbsp;label: "Label of the button",<br />&nbsp;&nbsp;click: function: { Function triggered on click }<br />}</pre><br />Note that in the 'click' function, 'this' will refer to the instance of Dialog. ||

== Event Table ==
|| *Name* || *Description* || *Parameters* ||
|| _onCancelButtonClickEvent_ ||  When the "CancelButton" is clicked ||  ||
|| _onOkButtonClickEvent_ ||  When the "OK" button is clicked ||  ||


