#summary Class API for String from module XPrototype
#labels Class-jplex.xprototype.String,Release-Pangolin,Module-XPrototype,API-Class
#sidebar APIPangolinxprototypeIndex

= Class *jplex.xprototype.String*  =

== Description ==
jPlex's XPrototype extended String





== Methods ==
<table border="1" cellspacing="0" cellpadding="5">                                
<tr><td> *Signature* </td><td> *Return Type* </td><td> *Description* </td></tr>
<tr><td> `lpad(length, character)` </td><td> `void` </td><td> Completes a string to a desired length with a character repeted to the left<br />  - `length <Integer>` Desired length of the result string<br/> - `character <String>` Optional. Character used to complete the string. Default is a blank space.<br/></td></tr>
<tr><td> `rpad(length, character)` </td><td> `String` </td><td> Completes a string to a desired length with a character repeted to the right<br />  - `length <Integer>` Desired length of the result string<br/> - `character <String>` Optional. Character used to complete the string. Default is a blank space.<br/></td></tr>

</table>
